# 科研练习项目说明书

> **定位**：这是一个"从数据集到完整研究报告"的科研练习项目——示范如何用公开数据完成一项 GIS+机器学习研究，并诚实评估其可靠性。
> **聚焦**：流程、方法与可复现性的学习价值。
> 原始素材：汶川震区杂谷脑河流域滑坡易发性评价（完整流水线 stage 1–26，全部真实运行结果）。

---

## 一、这个项目练什么（6 项科研技能）

1. **公开数据获取与预处理**：Copernicus DEM、MODIS NDVI（含 Earthdata 登录）、Zenodo 滑坡编目——不花一分钱拿到可用数据集；
2. **GIS 因子工程**：从 DEM 派生 11 类地形/水文因子；学会诊断**共线性**（TRI vs 坡度 r=0.943 → 剔除）；
3. **样本构建**：正负样本 1:1 平衡、空间缓冲剔除——理解"样本决定模型上限"；
4. **机器学习建模与对比**：LR / RF / XGBoost，5 折交叉验证 + **显著性检验**（McNemar/DeLong）+ 校准（Brier）；
5. **诚实的模型验证**：随机 CV ≠ 泛化能力——空间分箱 CV、连续空间块 + 缓冲、时间外推（2013-2021 滑坡）三层验证体系；
6. **可复现与审查**：固定种子、数据驱动制图、全流水线脚本 + README——**这是科研项目最值钱的部分**。

## 二、项目故事线（研究问题）

> 在强震区小流域，仅用公开数据（无 InSAR、无地质图），机器学习模型能多好地预测滑坡易发性？这种能力**跨空间**能保持多少、**跨时间**又能保持多少？

三个递进发现（练习的关键输出）：
- **随机 CV AUC 0.910**（XGBoost）——表面很漂亮；
- **空间 CV 降到 0.63–0.84**（视严格程度）——提醒"插值 ≠ 外推"；
- **时间外推 89–97% 新滑坡命中高/极高区**——模型确实学到了有用的空间结构。

练习的最高价值时刻：**亲手复现"一个漂亮数字被更严格的验证拆穿"的过程**。

## 三、目录结构

```
final/                          ← 本包
├── paper_draft_en.md           ← 完整研究叙述（“项目报告”范本）
├── 科研练习项目说明书.md        ← 本文件（定位与学习路径）
├── 复现教程.md                  ← 从零开始跑通全文的教程
├── 修订执行记录.md              ← 方法修订与自查逐项记录（示范如何自我审查）
├── 图件审查报告.md              ← 图件与正文一致性自查示范
├── figures/ (9 张)             ← 产出图件
├── code/ (17 个 stage + README) ← 流水线代码
└── data/ (8 个结果文件)         ← 每张表的源头
```

## 四、学习路径（建议顺序）

| 步骤 | 做什么 | 对应文件 | 学到什么 |
|------|--------|---------|---------|
| 1 | 搭环境、下载数据 | `code/REPRODUCIBILITY_README.md` | Python 地学栈、公开数据源 |
| 2 | 跑地形因子（stage1） | `code/stage1_terrain.py` | DEM → 坡度/曲率/TWI 等 |
| 3 | 跑样本（stage2b） | `code/stage2b_samples.py` | 平衡采样、缓冲剔除 |
| 4 | 跑模型（stage13/21b） | `code/stage13_final_v3.py` 等 | 建模、显著性、校准 |
| 5 | 跑空间 CV（stage18） | `code/stage18_rigorous_spatial_cv.py` | **空间自相关的坑** |
| 6 | 跑时间外推（stage19） | `code/stage19_temporal_extrapolation.py` | 独立验证的价值 |
| 7 | 跑可解释性与图件（stage17/26） | `code/stage17_shap.py` 等 | SHAP、可视化 |
| 8 | 对照 `paper_draft_en.md` 读全篇 | — | 完整的科研叙事怎么写 |

## 五、扩展练习（进阶玩法）

1. **换研究区**：把 DEM 瓦片换成一个你熟悉的地方（数据全公开）——整套流程直接复用；
2. **加因子**：拿到地质图后补"岩性"因子，观察它是否挤掉高程的主导地位；
3. **换模型**：加 SVM / LightGBM / 深度网络，重跑显著性检验；
4. **更严的空间验证**：尝试 leave-location-out（按子流域整块留出）替代网格分箱；
5. **写一份自己的研究小结**：按 `paper_draft_en.md` 的叙事结构，用不超过 2 页概括这项研究的问题、方法、发现与局限——锻炼"讲清楚一个研究"的能力。

## 六、诚实声明（项目自带的教育价值）

- 本项目的**结论口径经历过一次自我推翻**（99.56% 循环验证 → OOF 59.7%；0.911 → 0.910）——这是刻意的：**练习的目标之一就是学会发现并修正自己的验证漏洞**；
- 空间 CV 结果（0.63-0.84）比随机 CV（0.910）难看，但**如实保留**——诚实报告是科研第一课；
- 所有数字可从 `data/` 溯源，所有图件可从 `code/` 复现——**可复现性即可信度**。

---

*用途说明：可作为课程作业、本科生科研训练、答辩展示的数据源与代码基础。引用原始数据请署名数据源（Copernicus / NASA / Zenodo: 10.5281/zenodo.16418155）。*