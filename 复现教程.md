# 从零复现教程：GIS+ML 滑坡易发性完整流水线

> 目标：在一台普通电脑上，从零跑通"数据 → 因子 → 建模 → 验证 → 制图"的全流程。
> 预计时长：约 3–5 小时（含数据下载等待）。
> 前置：Python 3.10+、约 5 GB 磁盘、可联网。

---

## 第 0 步：环境准备（15 分钟）

```bash
# 核心包
pip install numpy pandas scikit-learn xgboost scipy matplotlib rasterio geopandas shap pysheds

# HDF4 解析（MODIS 数据用）：安装 QGIS 3.x（含 GDAL）或任意带 HDF4 驱动的 GDAL
# 可选：pyhdf（读取 HDF4 的备选，本项目主要用 QGIS GDAL）
```

**验证**：`python -c "import rasterio, xgboost, shap, geopandas; print('OK')"`

## 第 1 步：下载数据（30–60 分钟，视网速）

| 数据 | 在哪拿 | 指令 |
|------|--------|------|
| Copernicus DEM 30m | AWS 开放数据 | 下载 4 瓦片 N31-32E102-103 到 `data/dem/` |
| MODIS NDVI | NASA LP DAAC（Earthdata 注册） | 下载 2020 夏 5 期 + 2007 夏 3 期 MOD13Q1 (h26v05) |
| 滑坡编目 | Zenodo 10.5281/zenodo.16418155 | `data/landslide_dataset.zip`（含 2008-2021 各期） |

> 由于 Zenodo 直链可能需要浏览器头，教程建议：**直接用浏览器下载**，再放到脚本指定目录。

## 第 2 步：地形因子（stage1）

```bash
python code/stage1_terrain.py
```
- 做什么：拼接 4 瓦片 DEM → 裁剪研究区（102.75-103.65E / 31.15-31.85N）→ 计算高程/坡度/坡向/曲率/TWI/起伏度/SPI/STI + 距水系
- 产出：`outputs/elev.tif, slope.tif, ...`
- **可检查**：`slope mean ≈ 34°`（研究区是高山峡谷，坡陡是正常的）

**若想理解代码**：看 `stage1_terrain.py` 里"度→米"的换算——这是最容易犯错的地方（很多教程直接忽略纬度对距离的影响）。

## 第 3 步：NDVI 处理（stage9/10、stage20c）

```bash
python code/stage9_ndvi_download.py      # 下载 2020 NDVI（需 Earthdata token）
python code/stage10_ndvi_process.py      # HDF4 → 夏季中值 → 30m 栅格
python code/stage20c_ndvi2007_process.py # 2007 震前 NDVI（消融实验用）
```
- 产出：`outputs/ndvi.tif`（2020）、`outputs/ndvi_2007.tif`（2007）
- **注意**：HDF4 解析需要 QGIS GDAL（脚本里有 `gdal_translate` 调用）

## 第 4 步：样本构建（stage2b）

```bash
python code/stage2b_samples.py
```
- 做什么：滑坡多边形 → 质心正样本；非滑坡区随机负样本（**滑坡 60m 缓冲剔除**），1:1 平衡
- 产出：`outputs/samples_raw2.csv`
- **可检查**：正负样本各 ≈13,086

## 第 5 步：模型对比（stage13 + stage21b）

```bash
python code/stage13_final_v3.py          # LR/RF/XGB 随机 5 折 CV + ROC + 重要性
python code/stage21b_significance_fixed.py # McNemar/DeLong/Brier
```
- 预期结果：XGB AUC ≈ 0.910、Brier ≈ 0.118
- **学点**：为什么"XGB 比 LR 好 0.04"不能直接说"显著"？看 McNemar 与 DeLong 的差别。

## 第 6 步：空间 CV（stage18）—— 本项目的"灵魂"步骤

```bash
python code/stage18_rigorous_spatial_cv.py
```
- 做什么：三方案——网格分箱 / k-means 连续块+300m 缓冲 / 150m 敏感性，输出每折组成
- 预期：空间 CV AUC = **0.63–0.84**（远低于随机 CV 的 0.91）
- **学点（最重要）**：为什么随机 CV 高估？看 `cv_rigorous.json` 的 "folds" 字段——有 3 个块是纯负样本（单类折被排除）。
- 尝试：把缓冲从 300m 改成 1000m，看结果怎么变——**这就是 leave-location-out 的思想**。

## 第 7 步：时间外推（stage19）—— 最有说服力的独立验证

```bash
python code/stage19_temporal_extrapolation.py
```
- 做什么：用 2008 训练的模型，预测 2013/2015/2017/2019/2020/2021 的新滑坡落在高/极高区的比例
- 预期：**88.9–96.9%** 命中率（排除与 2008 重叠的多边形）
- **学点**：为什么这个验证比"自己测自己"可信？对比第 5 步的分区校验（OOF 59.7%）理解"循环验证"陷阱。

## 第 8 步：可解释性 + 制图（stage17/22/24b/26）

```bash
python code/stage17_shap.py              # SHAP 三图
python code/stage22_oof_zoning.py        # OOF 诚实分区（59.7%）
python code/stage24b_basin_watershed.py  # 流域掩膜（可选）
python code/stage26_fig2_v2.py           # 研究区图（含口径注）
```
- **学点**：SHAP 与 Gini 重要性结论一致 ≠ 因果——正样本空间聚集时，特征重要性可能只是位置代理（练习思考题）。

## 第 9 步：对照报告读全文

打开 `paper_draft_en.md`，对照上面每一步的结果：
- 表 3 ↔ 第 5 步
- 表 3b ↔ 第 5 步（显著性）
- 表 3c ↔ 第 6 步
- 表 4 ↔ 第 7 步
- 表 5 ↔ 第 3 步（NDVI 消融）
- 表 6 ↔ 第 8 步（OOF）

---

## 常见问题

| 问题 | 解法 |
|------|------|
| rasterio 打不开 HDF | 用 QGIS GDAL 的 gdal_translate（脚本内已调用）；或 pip install pyhdf |
| Earthdata 下载 403 | 需要带 `Authorization: Bearer <token>`（在 LP DAAC 网页生成） |
| 报 `np.in1d` 不存在 | pysheds 兼容问题：`np.in1d = np.isin`（脚本已内置） |
| 空间 CV 只有 2 折可用 | 正常！正样本空间聚集导致单类折——这正是要学的内容 |
| 我的 AUC 和教程不一样 | 检查 seed=42 是否一致、数据是否下载完整（尤其 2007 只用了 3 瓦片） |

## 完成标准（Checklist）

- [ ] `outputs/` 里能看到全部 .tif / .csv / .json / .png
- [ ] `model_results_final_v3.csv` 的 XGB AUC ≈ 0.910±0.006
- [ ] `cv_rigorous.json` 里能看出"2/5 折可用"与每折组成
- [ ] `temporal_extrapolation.csv` 六行命中率都在 88–97%
- [ ] 能向别人解释：为什么随机 CV、空间 CV、时间外推三个数字不一样

**学完这个项目，你就掌握了"数据驱动地质灾害研究"的完整方法论——这比任何一篇论文本身都值钱。**