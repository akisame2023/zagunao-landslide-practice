# 图件视觉 + 代码审查报告（v2.1）

> 审查方式：视觉链逐图检查（fig1-fig7）+ 生成代码硬编码数字全量审计 + 与稿件 v2.1 数字交叉核对
> 审查结论：**发现并修复 2 处图件-稿件不一致，其余全部通过**

---

## 一、视觉审查结果（7 张图）

| 图 | 内容 | 视觉结论 | 问题 |
|----|------|---------|------|
| fig1_flow | 技术路线 7 框 | ✅ 文字清晰无乱码、箭头逻辑完整 | — |
| fig2_study_area | 研究区+13,288 滑坡点 | ✅ 标题正确、橙色点沿河谷/坡地集中（符合地质常识） | — |
| fig3_roc_v3 | ROC 三曲线 | ⚠️ 原标注 XGB=0.911 | **已重渲染为 0.910** |
| fig4_importance_v3 | 特征重要性 | ✅ elev 最长、ndvi 第二、无重叠 | — |
| fig5_zones_v3 | 五级分区图 | ✅ 中文正常、蓝白红渐变、地形分界清晰 | — |
| fig6_cv_comparison | CV 对比柱状图 | ❌ 原空间柱为**旧值** 0.841/0.835/0.838 | **已重建（见下）** |
| fig7_shap_bar | SHAP 均值条 | ✅ Elevation>2.0 最长、NDVI~1.15 第二、可读 | — |

**视觉审查发现的真问题（1 个）**：fig6 的空间 CV 柱标的是 v1 旧值（0.841/0.835/0.838 = 早期网格分箱探索结果），与稿件 v2.1 表 3c（0.827/0.819/0.819）不符。

## 二、代码审查结果

### 硬编码数字全量审计（所有 stage*.py）
| 脚本 | 发现 | 处置 |
|------|------|------|
| stage16_fig6.py | **硬编码旧 AUC**（0.911/0.841/0.835/0.838） | 弃用，重写为数据驱动版 stage16b |
| stage22_oof_zoning.py | 99.56 仅出现在注释（批判性，合规） | 无需改 |
| 其余 stage 脚本 | 无硬编码展示数字（figs 均动态生成） | 通过 |

### 口径统一（0.910 vs 0.911 之争）
- **根因**：stage13 与 stage21b 同 seed 重跑，AUC 有 0.9102 vs 0.9108 的浮点级差异，两份稿件一度混用
- **决策**：统一以 stage21b（配显著性检验）为权威 → **XGB AUC = 0.910±0.006**
- **已落实**：
  - `model_results_final_v3.csv`（XGB 行统一为 0.910±0.006）
  - fig3 重渲染（图例 0.910）
  - fig6 数据驱动重渲染（随机柱 0.910）
  - 稿件两版早已用 0.910±0.006 ✓

### 图件生成脚本最终状态
| 文件 | 用途 | 状态 |
|------|------|------|
| stage16b_fig6_data_driven.py | **fig6 新生成器**（读 CSV+JSON，杜绝再次脱节） | ✅ 新增 |
| stage16c_fig3_relabel.py | fig3 统一口径渲染 | ✅ 新增 |
| stage13_final_v3.py | fig3 原生成器/模型主体 | 保留（发布口径以 stage21b 为准） |

## 三、修复后图件数值（与稿件 v2.1 完全一致）

```
fig3:  LR 0.873 / RF 0.902 / XGB 0.910
fig6:  随机 0.873 / 0.902 / 0.910
       空间 0.827 / 0.819 / 0.819   ← 与表 3c 网格方案一致
fig4/5/7: 与特征重要性/SHAP/分区一致
```

## 四、不确定性说明（视觉预算限制）

1. **fig3/fig6 重渲染后未经视觉复检**（视觉时间预算在本轮耗尽）——其数值由代码生成逻辑保证（读取统一 CSV/JSON），但**像素级显示未二次确认**，建议下一轮视觉恢复后抽查这两张；
2. fig7 的 summary/dependence 两张子图本轮只检了 bar 图，未逐张检（此前轮次已检过 summary，dependence 未检）；
3. fig2 的 13,288 标题数字与报告统一口径 13,086（去重后）并存——fig2 展示的是原始编目点数（图题"N=13288"），与摘要"13,086 正样本"存在口径差，已在报告 2.2 说明（去重/缺失剔除），图题不改（图题描述的是编目点数，非建模样本数），但**使用前建议在 fig2 图题加注"13,288 = inventory polygons"消除歧义**（注：final 包中的 fig2 已按 v2 加注版重渲染）。

## 五、结论

- **视觉**：6/7 张原图合格 + 1 张（fig6）发现问题
- **代码**：1 处硬编码旧值（已消除），1 处双口径（已统一）
- **修复后**：全部图件数字与稿件 v2.1 完全一致，且 fig6/fig3 改为数据驱动生成，**此类脱节不会再发生**